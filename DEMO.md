# ZetaFlow AI 驱动的跨链资产配置器 - 演示指南

## 已完成功能 ✅

### 1. AI 意图解析系统
**位置**: `front/src/lib/intent-parser.ts`

**功能**: 从自然语言解析资产配置意图
- 支持百分比权重解析（如"50% BTC, 30% ETH, 20% 风险资产"）
- 解析预算约束（如"2000 USDC 预算"）
- 解析风险偏好（如"最大滑点 0.5%"）
- 解析目标链偏好（如"优先 Ethereum/Bitcoin"）

**测试示例**:
```
输入：把我2000 USDC配置为50% BTC，30% ETH，20%风险资产，最大滑点0.5%
输出：RebalanceIntent {
  type: "rebalance",
  targets: [
    { symbol: "BTC", weight: 0.5, dstChain: "bitcoin" },
    { symbol: "ETH", weight: 0.3, dstChain: "ethereum" },
    { tag: "high_risk", weight: 0.2, basket: ["ARB", "OP", "SOL", "MATIC"] }
  ],
  budget: { symbol: "USDC", amount: 2000 },
  constraints: { maxSlippageBps: 50, maxTxCount: 6 }
}
```

### 2. 智能计划生成器
**位置**: `front/src/lib/plan-generator.ts`

**功能**: 将意图转换为可执行的跨链交易计划
- 当前余额聚合与目标权重计算
- 最小换手路径优化（减少交易数量和费用）
- 费用估算与时间预估
- 风险评级与成功概率计算
- 计划验证（权重总和、预算充足性、步骤数限制）

**输出**: ExecutablePlan 包含：
- 详细执行步骤（每步的 from/to token, 链路径, 费用, 时间）
- 总体摘要（总费用、总时间、风险等级、成功概率）
- ABI 编码的 callData（供 Gateway 调用）

### 3. 交互式计划摘要界面
**位置**: `front/src/pages/trade/trade.tsx` - `RebalancePlanModal`

**功能**: 用户友好的计划确认界面
- 配置目标可视化（权重饼图式展示）
- 执行步骤详细列表（链路径、时间、费用）
- 风险与费用摘要（总费用、预计时间、风险等级、成功概率）
- 验证错误提示（权重不平衡、预算不足等）
- 一键执行或取消操作

### 4. 扩展的聊天系统
**位置**: `front/src/pages/trade/components/Chat.tsx`

**功能**: 集成资产配置功能的AI对话
- 保留原有的 swap/balance/create_wallet 功能
- 新增 rebalance 意图处理
- 实时生成计划并展示摘要
- 错误处理与用户友好提示
- AI/命令模式切换

## 演示流程 🎯

### 步骤 1: 启动项目
```bash
cd /home/amyseer/zeta/zetaFlow/front
npm install
npm run dev
```

### 步骤 2: 测试意图解析
在聊天界面输入以下测试指令：

**基础配置**:
```
把我2000 USDC配置为50% BTC，30% ETH，20%风险资产
```

**带约束条件**:
```
重平衡到70% ETH, 30% 稳定币，预算1000 USDC，最大滑点0.3%
```

**指定目标链**:
```
配置 40% BTC 在 Bitcoin，40% ETH 在 Ethereum，20% Layer2
```

### 步骤 3: 查看生成的计划
- AI 解析意图并生成执行计划
- 点击"查看计划"打开详细摘要模态
- 检查配置目标、执行步骤、费用估算、风险评级
- 确认或取消执行

### 步骤 4: 验证边界情况
**权重不平衡**:
```
配置 40% BTC，40% ETH  // 总权重 80%，验证失败
```

**不支持的资产**:
```
配置 50% 未知币，50% ETH
```

## 技术亮点 🚀

### 1. 渐进式解析架构
- **第一层**: 正则表达式 + 关键词匹配提取结构化意图
- **第二层**: 基于余额和目标权重的计划生成与优化
- **第三层**: ABI 编码生成可执行的链上 callData

### 2. 用户体验优化
- **即时预览**: 输入时实时显示解析结果
- **人类可读**: 所有技术细节转换为用户友好的描述
- **错误处理**: 详细的验证错误提示和修复建议
- **优雅降级**: AI 模式失败时自动回退到命令解析

### 3. 可扩展设计
- **模块化**: 意图解析、计划生成、UI 组件完全解耦
- **类型安全**: 完整的 TypeScript 类型定义
- **配置驱动**: 支持的链、代币、路由器通过配置文件管理

## 下一步开发 📋

### 即将完成 (优先级高)
1. **钱包集成**: wagmi + viem 连接 MetaMask，支持网络切换
2. **配置模块**: 创建 chains.ts, addresses.ts 配置 Gateway 地址
3. **Gateway 调用**: 实现连接链存入 + callData 的实际交易发送

### 后续功能 (优先级中)
4. **Universal App 合约**: ZetaChain 上的计划接收与执行合约
5. **合约部署**: 部署到 ZetaChain 测试网并配置地址
6. **端到端测试**: 完整的跨链流程验证

### 增强功能 (优先级低)
- 实时价格数据集成
- 更复杂的路由优化算法
- 多资产同时处理支持
- 历史计划追踪与分析

## AI 价值体现 💡

相比传统的单次 swap 操作，ZetaFlow 的 AI 系统提供：

1. **复杂约束求解**: 从多维约束（预算、滑点、时间、链偏好）中找到最优解
2. **路径规划**: 自动计算最小换手的跨链交易路径
3. **风险评估**: 基于步骤复杂度和历史数据的智能风险评级
4. **自然交互**: 用户无需了解技术细节，通过对话即可完成复杂配置

这正是 **AI × ZetaChain 通用能力** 的完美结合：AI 负责理解与规划，ZetaChain 负责跨链执行与回滚保障。

---

**当前状态**: 前端 AI 意图解析与计划生成已完成，可进行完整的演示流程。下一步是钱包集成与实际链上交易的实现。


